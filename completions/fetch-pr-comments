_kxue43_fetch_pr_comments::complete() {
  if ((COMP_CWORD == 1)) && [[ $2 == @(|-|-h) ]]; then
    COMPREPLY=("-h")
  fi

  return 0
} && complete -o bashdefault -F _kxue43_fetch_pr_comments::complete fetch-pr-comments

# vim: set ft=bash :
