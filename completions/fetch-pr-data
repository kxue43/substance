_kxue43_fetch_pr_data::complete() {
  if ((COMP_CWORD == 1)) && [[ $2 == @(|-|-h) ]]; then
    COMPREPLY=("-h")
  fi

  return 0
} && complete -o bashdefault -F _kxue43_fetch_pr_data::complete fetch-pr-data

# vim: set ft=bash :
